---
title: 1.LangChain-基本介绍
categories: LangChain
date: 2026-03-19 21:30:00
tags: Agent开发
---

# 快速上手
## pnpm 安装

```bash
iwr https:#get.pnpm.io/install.ps1 -useb | iex
```

## Dom实例
```bash
## 创建dom文件初始化项目
pnpm init
# 安装langchain 依赖ollama模型
pnpm add @langchain/ollama
```

**快速上手**
```bash
import {Ollama} from "@langchain/ollama";

# 创建模型实例
const model =new Ollama({
  model: "llama3",
});

const prompt = "你是一位中文智能助手，请你用中文回答Ai对人类文明的影响";

const res = await model.invoke(prompt)

console.log(res)
```

** 流式输出**
```bash
import {Ollama} from "@langchain/ollama";

# 创建模型实例
const model =new Ollama({
  model: "llama3",
});

const prompt = "你是一位中文智能助手，请你用中文简体回答Ai对人类文明的影响";

const res = await model.stream(prompt)
console.log(res)
for await(const chunk of res) {
  process.stdout.write(chunk)
}
```

# 模型接口
LangChain 支持两类主语言模型
1. 文本补全模型
2. 对话模型

## 文本补全模型
上面的案例

## 对话模型
整个LangChin生态由多个模块包组成，构建完整的智能应用开发
#### 1. @langchain/core
LangChain的“内核”， 提供最基础的抽象能力，比如`Runnable`（可执行单元），`Prompt`（提示词模板）、`OutputParser`（输出解析器）等，同时还包含LangChain Expression Language,用于编排复杂的链式逻辑。

#### 2. @langchain/community
社区维护的扩展包，收集第三方集成，比如各类向量数据库、模型api、文档加载器，类似插件市场

#### 3. langchain
在核心能力基础上的高阶封装，内置肠胃的链式组件（chains）、智能体模块（agents）、工具条度（tools）等，大多数langchain应用默认入口包


```bash
pnpm add @langchain/core
```

案例
```bash
import {ChatOllama} from "@langchain/ollama";
import { 
  HumanMessagePromptTemplate,
  SystemMessagePromptTemplate,
  ChatPromptTemplate
} from "@langchain/core/prompts";

# 创建模型实例
const model =new ChatOllama({
  model: "llama3",
  temperature: 0.7,
  stream: true,
});

# 创建多角色提示词模板

# 1.创建系统提示词
const sysPrompt = SystemMessagePromptTemplate.fromTemplate("你是一个有一个翻译助理，请将用户的输入的内容由{input_language}直接翻译翻译成{output_language}。")

# 2.创建用户提示词
const userPrompt = HumanMessagePromptTemplate.fromTemplate("{text}")
console.log(sysPrompt);

# 3.合成提示词
const chatPrompt = ChatPromptTemplate.fromMessages([sysPrompt, userPrompt]);

# 4.填充变量
const messages = await chatPrompt.formatMessages({
  input_language: "中文",
  output_language: "英文",
  text: "今天天气正好，我们去游泳吧"
})

const res = await model.stream(messages)

for await(const chunk of res) {
  process.stdout.write(chunk.content)
}
```

# 缓存
1.快速上手
```bash
# 下载cache
# pnpm add cache
const model =new Ollama({
  model: "llama3",
  cache: true, # 开启缓存
})
```

2.解决缓存默认使用的哈希算法不够安全问题
自定义缓存类，解决官方提示问题，现在不需要解决，可以看下思路
```bash
# 创建utils.js方法
import crypto from "crypto";

export class SecureCache {
  constructor () {
    this.cache = new Map(); # 使用 Map 来存储缓存数据
  }

  # 生成缓存键
  _createSecureKey (prompt, llmString) {
    const data = `${prompt}-${llmString}`;
    return crypto.createHash("sha256").update(data).digest("hex");
  }

  # 获取缓存数据
  async lookup(prompt, llmString) {
    const key = this._createSecureKey(prompt, llmString);
    return this.cache.get(key);
  }

  async update(prompt, llmString, result) {
    const key = this._createSecureKey(prompt, llmString);
    this.cache.set(key, result);
  }

  async clear() {
    this.cache.clear();
  }
}

# index.js 使用
import {SecureCache} from "./utils.js"; 

const sc = new SecureCache();

const model =new Ollama({
  model: "llama3",
  cache: sc,
})
```

## 综合案例
1. 提升响应速度：重复问题无需重新调用模型，直接命中缓存
2. 降低调用成本，尤其对于调用远程的，可节省费用
3. 避免冗余：减少系统负担，提高整体并发性能

```bash
# 补充安装，对比向量
# pnpm add cosine-similarity


import { ChatOllama, OllamaEmbeddings } from "@langchain/ollama";
import cosineSimilarity from "cosine-similarity";

const model = new ChatOllama({
  model: "llama3",
  stream: true,
});

# 负责将文本装换成向量的模型
const embedder = new OllamaEmbeddings({
  model: "nomic-embed-text",
});

const cache = []; # 用于存储缓存的向量和对应的文本

async function callLLM(inputText, threshold = 0.7) {
  # 1. 将输入文本转换为向量
  const queryEmbedding = await embedder.embedQuery(inputText);

  # 2. 检查缓存中是否有相似的向量
  # 当前用户问题的向量和缓存里面的每一项做比较
  for (const item of cache) {
    const score = cosineSimilarity(queryEmbedding, item.embedding); # 计算相似度
    if (score >= threshold) {
      # `命中缓存，使用缓存的响应， 相似度：${score}`
      return process.stdout.write(item.response); # 返回缓存的响应
    }
  }
  let fullResponse = "";
  const stream = await model.stream(inputText); # 调用模型获取响应
  for await (const chunk of stream) {
    process.stdout.write(chunk.content); # 实时输出流数据
    fullResponse += chunk.content; # 拼接完整响应
  }

  cache.push({
    input: inputText, # 存储原始输入文本
    embedding: queryEmbedding, # 存储向量
    response: fullResponse, # 存储模型的响应
  });
}

const q1 = "请你使用中文介绍下大象这种动物";
const q2 = "请你使用中文介绍下大象这种动物";
await callLLM(q1);

console.log("\n\n第二次调用：");
await callLLM(q2);
```